/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __mars_message_AppSetRoomProperties_hpp__
#define __mars_message_AppSetRoomProperties_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>

namespace mars_message
{

class AppSetRoomProperties
{
    public:
        int8_t     version;

        int8_t     count;

        std::vector< int8_t > roomId;

        std::vector< int32_t > fan;

        std::vector< int32_t > waterLevel;

        std::vector< int8_t > yMop;

        std::vector< int8_t > cleanRepeat;

#ifdef __NLOHMANN_JSON_CPP
    public:
        NLOHMANN_DEFINE_TYPE_INTRUSIVE(AppSetRoomProperties, version, count, roomId, fan, waterLevel, yMop, cleanRepeat)
#endif
#ifdef __MSGPACK
    public:
        MSGPACK_DEFINE(AppSetRoomProperties, version, count, roomId, fan, waterLevel, yMop, cleanRepeat)
#endif
    public:
        bool operator==(const AppSetRoomProperties& other) const
        {
            return version == other.version && count == other.count && roomId == other.roomId && fan == other.fan && waterLevel == other.waterLevel && yMop == other.yMop && cleanRepeat == other.cleanRepeat;
        }
    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "AppSetRoomProperties"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int AppSetRoomProperties::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int AppSetRoomProperties::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int AppSetRoomProperties::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t AppSetRoomProperties::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* AppSetRoomProperties::getTypeName()
{
    return "AppSetRoomProperties";
}

int AppSetRoomProperties::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->version, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->count, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->count > 0) {
        tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->roomId[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count > 0) {
        tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->fan[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count > 0) {
        tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->waterLevel[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count > 0) {
        tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->yMop[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count > 0) {
        tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->cleanRepeat[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int AppSetRoomProperties::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->version, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->count, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->count) {
        this->roomId.resize(this->count);
        tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->roomId[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count) {
        this->fan.resize(this->count);
        tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->fan[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count) {
        this->waterLevel.resize(this->count);
        tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->waterLevel[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count) {
        this->yMop.resize(this->count);
        tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->yMop[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->count) {
        this->cleanRepeat.resize(this->count);
        tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->cleanRepeat[0], this->count);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int AppSetRoomProperties::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, this->count);
    enc_size += __int32_t_encoded_array_size(NULL, this->count);
    enc_size += __int32_t_encoded_array_size(NULL, this->count);
    enc_size += __int8_t_encoded_array_size(NULL, this->count);
    enc_size += __int8_t_encoded_array_size(NULL, this->count);
    return enc_size;
}

uint64_t AppSetRoomProperties::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0x0f9cc86a4f613656LL;
    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
