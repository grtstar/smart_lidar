"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

import mars_message.Point

class Path(object):
    __slots__ = ["count", "points"]

    __typenames__ = ["int16_t", "mars_message.Point"]

    __dimensions__ = [None, ["count"]]

    def __init__(self):
        self.count = 0
        self.points = []

    def encode(self):
        buf = BytesIO()
        buf.write(Path._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">h", self.count))
        for i0 in range(self.count):
            assert self.points[i0]._get_packed_fingerprint() == mars_message.Point._get_packed_fingerprint()
            self.points[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Path._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Path._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = Path()
        self.count = struct.unpack(">h", buf.read(2))[0]
        self.points = []
        for i0 in range(self.count):
            self.points.append(mars_message.Point._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if Path in parents: return 0
        newparents = parents + [Path]
        tmphash = (0x2e56362f5c12686f+ mars_message.Point._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if Path._packed_fingerprint is None:
            Path._packed_fingerprint = struct.pack(">Q", Path._get_hash_recursive([]))
        return Path._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Path._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.count = j["count"]
        self.points = [x.from_dict() for x in j["points"]]

    def to_dict(self):
        j = {}
        j["count"] = self.count
        j["points"] = [x.to_dict() for x in self.points]
        return j

