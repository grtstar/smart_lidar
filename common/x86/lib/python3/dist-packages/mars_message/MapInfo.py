"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

class MapInfo(object):
    __slots__ = ["createTime", "mapSign", "editTime", "name", "isFormalMap", "isSaveToFlash"]

    __typenames__ = ["int32_t", "int32_t", "int32_t", "string", "int8_t", "int8_t"]

    __dimensions__ = [None, None, None, None, None, None]

    def __init__(self):
        self.createTime = 0
        self.mapSign = 0
        self.editTime = 0
        self.name = ""
        self.isFormalMap = 0
        self.isSaveToFlash = 0

    def encode(self):
        buf = BytesIO()
        buf.write(MapInfo._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">iii", self.createTime, self.mapSign, self.editTime))
        __name_encoded = self.name.encode('utf-8')
        buf.write(struct.pack('>I', len(__name_encoded)+1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">bb", self.isFormalMap, self.isSaveToFlash))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != MapInfo._get_packed_fingerprint():
            raise ValueError("Decode error")
        return MapInfo._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = MapInfo()
        self.createTime, self.mapSign, self.editTime = struct.unpack(">iii", buf.read(12))
        __name_len = struct.unpack('>I', buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode('utf-8', 'replace')
        self.isFormalMap, self.isSaveToFlash = struct.unpack(">bb", buf.read(2))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if MapInfo in parents: return 0
        tmphash = (0xb62a0f0823cf5107) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if MapInfo._packed_fingerprint is None:
            MapInfo._packed_fingerprint = struct.pack(">Q", MapInfo._get_hash_recursive([]))
        return MapInfo._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", MapInfo._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.createTime = j["createTime"]
        self.mapSign = j["mapSign"]
        self.editTime = j["editTime"]
        self.name = j["name"]
        self.isFormalMap = j["isFormalMap"]
        self.isSaveToFlash = j["isSaveToFlash"]

    def to_dict(self):
        j = {}
        j["createTime"] = self.createTime
        j["mapSign"] = self.mapSign
        j["editTime"] = self.editTime
        j["name"] = self.name
        j["isFormalMap"] = self.isFormalMap
        j["isSaveToFlash"] = self.isSaveToFlash
        return j

