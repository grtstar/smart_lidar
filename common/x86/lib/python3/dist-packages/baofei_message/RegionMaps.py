"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

import baofei_message.RegionMap

class RegionMaps(object):
    __slots__ = ["count", "map"]

    __typenames__ = ["int32_t", "baofei_message.RegionMap"]

    __dimensions__ = [None, ["count"]]

    def __init__(self):
        self.count = 0
        self.map = []

    def encode(self):
        buf = BytesIO()
        buf.write(RegionMaps._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.count))
        for i0 in range(self.count):
            assert self.map[i0]._get_packed_fingerprint() == baofei_message.RegionMap._get_packed_fingerprint()
            self.map[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != RegionMaps._get_packed_fingerprint():
            raise ValueError("Decode error")
        return RegionMaps._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = RegionMaps()
        self.count = struct.unpack(">i", buf.read(4))[0]
        self.map = []
        for i0 in range(self.count):
            self.map.append(baofei_message.RegionMap._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if RegionMaps in parents: return 0
        newparents = parents + [RegionMaps]
        tmphash = (0xc4086fcbedcc1e4b+ baofei_message.RegionMap._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if RegionMaps._packed_fingerprint is None:
            RegionMaps._packed_fingerprint = struct.pack(">Q", RegionMaps._get_hash_recursive([]))
        return RegionMaps._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", RegionMaps._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.count = j["count"]
        self.map = [x.from_dict() for x in j["map"]]

    def to_dict(self):
        j = {}
        j["count"] = self.count
        j["map"] = [x.to_dict() for x in self.map]
        return j

