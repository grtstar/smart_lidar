#!/usr/bin/python

import lcm
import mars_message

def toEvent(evt):
    return ord(evt[0])<<24 | ord(evt[1])<<16 | ord(evt[2])<<8 | ord(evt[3])


e:mars_message.Event  = mars_message.Event()
e.event = toEvent('CKEY')
e.param = 0
lc = lcm.LCM()
lc.publish("Mevt", e.encode())