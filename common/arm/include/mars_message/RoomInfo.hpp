/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __mars_message_RoomInfo_hpp__
#define __mars_message_RoomInfo_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "mars_message/GridMap.hpp"
#include "mars_message/DoorInfo.hpp"
#include "mars_message/RoomPropety.hpp"

namespace mars_message
{

class RoomInfo
{
    public:
        mars_message::GridMap roomMap;

        int32_t    doorCount;

        std::vector< mars_message::DoorInfo > doors;

        int32_t    roomNum;

        std::vector< mars_message::RoomPropety > roomPropeties;

        std::vector< int32_t > cleanOrder;

#ifdef __NLOHMANN_JSON_CPP
    public:
        NLOHMANN_DEFINE_TYPE_INTRUSIVE(RoomInfo, roomMap, doorCount, doors, roomNum, roomPropeties, cleanOrder)
#endif
    public:
        bool operator==(const RoomInfo& other) const
        {
            return roomMap == other.roomMap && doorCount == other.doorCount && doors == other.doors && roomNum == other.roomNum && roomPropeties == other.roomPropeties && cleanOrder == other.cleanOrder;
        }
    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "RoomInfo"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int RoomInfo::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int RoomInfo::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int RoomInfo::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t RoomInfo::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* RoomInfo::getTypeName()
{
    return "RoomInfo";
}

int RoomInfo::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->roomMap._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->doorCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->doorCount; a0++) {
        tlen = this->doors[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->roomNum, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->roomNum; a0++) {
        tlen = this->roomPropeties[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->roomNum > 0) {
        tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->cleanOrder[0], this->roomNum);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int RoomInfo::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->roomMap._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->doorCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->doors.resize(this->doorCount);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->doorCount; a0++) {
        tlen = this->doors[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->roomNum, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->roomPropeties.resize(this->roomNum);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->roomNum; a0++) {
        tlen = this->roomPropeties[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->roomNum) {
        this->cleanOrder.resize(this->roomNum);
        tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->cleanOrder[0], this->roomNum);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int RoomInfo::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->roomMap._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->doorCount; a0++) {
        enc_size += this->doors[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->roomNum; a0++) {
        enc_size += this->roomPropeties[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, this->roomNum);
    return enc_size;
}

uint64_t RoomInfo::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == RoomInfo::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, RoomInfo::getHash };

    uint64_t hash = 0xfd92cd7f16a5d8a1LL +
         mars_message::GridMap::_computeHash(&cp) +
         mars_message::DoorInfo::_computeHash(&cp) +
         mars_message::RoomPropety::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
