/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __baofei_message_Forbbiden_hpp__
#define __baofei_message_Forbbiden_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "baofei_message/Line2d.hpp"
#include "baofei_message/Rect2d.hpp"
#include "baofei_message/Rect2d.hpp"

namespace baofei_message
{

class Forbbiden
{
    public:
        int8_t     lineCount;

        std::vector< baofei_message::Line2d > wall;

        int8_t     areaCount;

        std::vector< baofei_message::Rect2d > area;

        std::vector< int32_t > areaSign;

        int8_t     mopCount;

        std::vector< baofei_message::Rect2d > mop;

        std::vector< int32_t > mopSign;

#ifdef __NLOHMANN_JSON_CPP
    public:
        NLOHMANN_DEFINE_TYPE_INTRUSIVE(Forbbiden, lineCount, wall, areaCount, area, areaSign, mopCount, mop, mopSign)
#endif
#ifdef __MSGPACK
    public:
        MSGPACK_DEFINE(Forbbiden, lineCount, wall, areaCount, area, areaSign, mopCount, mop, mopSign)
#endif
    public:
        bool operator==(const Forbbiden& other) const
        {
            return lineCount == other.lineCount && wall == other.wall && areaCount == other.areaCount && area == other.area && areaSign == other.areaSign && mopCount == other.mopCount && mop == other.mop && mopSign == other.mopSign;
        }
    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "Forbbiden"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int Forbbiden::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int Forbbiden::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int Forbbiden::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t Forbbiden::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* Forbbiden::getTypeName()
{
    return "Forbbiden";
}

int Forbbiden::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->lineCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->lineCount; a0++) {
        tlen = this->wall[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->areaCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->areaCount; a0++) {
        tlen = this->area[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->areaCount > 0) {
        tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->areaSign[0], this->areaCount);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->mopCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->mopCount; a0++) {
        tlen = this->mop[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->mopCount > 0) {
        tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->mopSign[0], this->mopCount);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int Forbbiden::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->lineCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->wall.resize(this->lineCount);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->lineCount; a0++) {
        tlen = this->wall[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->areaCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->area.resize(this->areaCount);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->areaCount; a0++) {
        tlen = this->area[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->areaCount) {
        this->areaSign.resize(this->areaCount);
        tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->areaSign[0], this->areaCount);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->mopCount, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->mop.resize(this->mopCount);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->mopCount; a0++) {
        tlen = this->mop[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    if(this->mopCount) {
        this->mopSign.resize(this->mopCount);
        tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->mopSign[0], this->mopCount);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int Forbbiden::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->lineCount; a0++) {
        enc_size += this->wall[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->areaCount; a0++) {
        enc_size += this->area[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, this->areaCount);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->mopCount; a0++) {
        enc_size += this->mop[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, this->mopCount);
    return enc_size;
}

uint64_t Forbbiden::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == Forbbiden::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, Forbbiden::getHash };

    uint64_t hash = 0xf6b339b198f60059LL +
         baofei_message::Line2d::_computeHash(&cp) +
         baofei_message::Rect2d::_computeHash(&cp) +
         baofei_message::Rect2d::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
