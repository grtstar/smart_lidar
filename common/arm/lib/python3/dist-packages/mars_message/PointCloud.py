"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

import mars_message.Point

class PointCloud(object):
    __slots__ = ["timestampMs", "count", "points", "intensities"]

    __typenames__ = ["int64_t", "int32_t", "mars_message.Point", "float"]

    __dimensions__ = [None, None, ["count"], ["count"]]

    def __init__(self):
        self.timestampMs = 0
        self.count = 0
        self.points = []
        self.intensities = []

    def encode(self):
        buf = BytesIO()
        buf.write(PointCloud._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.timestampMs, self.count))
        for i0 in range(self.count):
            assert self.points[i0]._get_packed_fingerprint() == mars_message.Point._get_packed_fingerprint()
            self.points[i0]._encode_one(buf)
        buf.write(struct.pack('>%df' % self.count, *self.intensities[:self.count]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != PointCloud._get_packed_fingerprint():
            raise ValueError("Decode error")
        return PointCloud._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = PointCloud()
        self.timestampMs, self.count = struct.unpack(">qi", buf.read(12))
        self.points = []
        for i0 in range(self.count):
            self.points.append(mars_message.Point._decode_one(buf))
        self.intensities = struct.unpack('>%df' % self.count, buf.read(self.count * 4))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if PointCloud in parents: return 0
        newparents = parents + [PointCloud]
        tmphash = (0xfa2a3d35529337a9+ mars_message.Point._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if PointCloud._packed_fingerprint is None:
            PointCloud._packed_fingerprint = struct.pack(">Q", PointCloud._get_hash_recursive([]))
        return PointCloud._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", PointCloud._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.timestampMs = j["timestampMs"]
        self.count = j["count"]
        self.points = [x.from_dict() for x in j["points"]]
        self.intensities = j["intensities"]

    def to_dict(self):
        j = {}
        j["timestampMs"] = self.timestampMs
        j["count"] = self.count
        j["points"] = [x.to_dict() for x in self.points]
        j["intensities"] = self.intensities
        return j

