"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

class Bumper(object):
    __slots__ = ["timestampMs", "count", "mask"]

    __typenames__ = ["int64_t", "int8_t", "int32_t"]

    __dimensions__ = [None, None, None]

    def __init__(self):
        self.timestampMs = 0
        self.count = 0
        self.mask = 0

    def encode(self):
        buf = BytesIO()
        buf.write(Bumper._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qbi", self.timestampMs, self.count, self.mask))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Bumper._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Bumper._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = Bumper()
        self.timestampMs, self.count, self.mask = struct.unpack(">qbi", buf.read(13))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if Bumper in parents: return 0
        tmphash = (0xd1a1ed4518287754) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if Bumper._packed_fingerprint is None:
            Bumper._packed_fingerprint = struct.pack(">Q", Bumper._get_hash_recursive([]))
        return Bumper._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Bumper._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.timestampMs = j["timestampMs"]
        self.count = j["count"]
        self.mask = j["mask"]

    def to_dict(self):
        j = {}
        j["timestampMs"] = self.timestampMs
        j["count"] = self.count
        j["mask"] = self.mask
        return j

