"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

import mars_message.Polygon

class AppRestrictedArea(object):
    __slots__ = ["version", "count", "mode", "type", "polygon", "name"]

    __typenames__ = ["int8_t", "int8_t", "int8_t", "int8_t", "mars_message.Polygon", "string"]

    __dimensions__ = [None, None, ["count"], ["count"], ["count"], ["count"]]

    def __init__(self):
        self.version = 0
        self.count = 0
        self.mode = []
        self.type = []
        self.polygon = []
        self.name = []

    def encode(self):
        buf = BytesIO()
        buf.write(AppRestrictedArea._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bb", self.version, self.count))
        buf.write(struct.pack('>%db' % self.count, *self.mode[:self.count]))
        buf.write(struct.pack('>%db' % self.count, *self.type[:self.count]))
        for i0 in range(self.count):
            assert self.polygon[i0]._get_packed_fingerprint() == mars_message.Polygon._get_packed_fingerprint()
            self.polygon[i0]._encode_one(buf)
        for i0 in range(self.count):
            __name_encoded = self.name[i0].encode('utf-8')
            buf.write(struct.pack('>I', len(__name_encoded)+1))
            buf.write(__name_encoded)
            buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != AppRestrictedArea._get_packed_fingerprint():
            raise ValueError("Decode error")
        return AppRestrictedArea._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = AppRestrictedArea()
        self.version, self.count = struct.unpack(">bb", buf.read(2))
        self.mode = struct.unpack('>%db' % self.count, buf.read(self.count))
        self.type = struct.unpack('>%db' % self.count, buf.read(self.count))
        self.polygon = []
        for i0 in range(self.count):
            self.polygon.append(mars_message.Polygon._decode_one(buf))
        self.name = []
        for i0 in range(self.count):
            __name_len = struct.unpack('>I', buf.read(4))[0]
            self.name.append(buf.read(__name_len)[:-1].decode('utf-8', 'replace'))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if AppRestrictedArea in parents: return 0
        newparents = parents + [AppRestrictedArea]
        tmphash = (0x2fb88f43305d3af2+ mars_message.Polygon._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if AppRestrictedArea._packed_fingerprint is None:
            AppRestrictedArea._packed_fingerprint = struct.pack(">Q", AppRestrictedArea._get_hash_recursive([]))
        return AppRestrictedArea._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", AppRestrictedArea._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.version = j["version"]
        self.count = j["count"]
        self.mode = j["mode"]
        self.type = j["type"]
        self.polygon = [x.from_dict() for x in j["polygon"]]
        self.name = j["name"]

    def to_dict(self):
        j = {}
        j["version"] = self.version
        j["count"] = self.count
        j["mode"] = self.mode
        j["type"] = self.type
        j["polygon"] = [x.to_dict() for x in self.polygon]
        j["name"] = self.name
        return j

