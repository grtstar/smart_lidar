"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

class Iap(object):
    __slots__ = ["packNum", "count", "code", "crc"]

    __typenames__ = ["int16_t", "int16_t", "int8_t", "int8_t"]

    __dimensions__ = [None, None, ["count"], None]

    def __init__(self):
        self.packNum = 0
        self.count = 0
        self.code = []
        self.crc = 0

    def encode(self):
        buf = BytesIO()
        buf.write(Iap._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">hh", self.packNum, self.count))
        buf.write(struct.pack('>%db' % self.count, *self.code[:self.count]))
        buf.write(struct.pack(">b", self.crc))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Iap._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Iap._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = Iap()
        self.packNum, self.count = struct.unpack(">hh", buf.read(4))
        self.code = struct.unpack('>%db' % self.count, buf.read(self.count))
        self.crc = struct.unpack(">b", buf.read(1))[0]
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if Iap in parents: return 0
        tmphash = (0xb36719ad57f68a43) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if Iap._packed_fingerprint is None:
            Iap._packed_fingerprint = struct.pack(">Q", Iap._get_hash_recursive([]))
        return Iap._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Iap._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.packNum = j["packNum"]
        self.count = j["count"]
        self.code = j["code"]
        self.crc = j["crc"]

    def to_dict(self):
        j = {}
        j["packNum"] = self.packNum
        j["count"] = self.count
        j["code"] = self.code
        j["crc"] = self.crc
        return j

