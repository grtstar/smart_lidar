"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

class Debug(object):
    __slots__ = ["cmd"]

    __typenames__ = ["string"]

    __dimensions__ = [None]

    def __init__(self):
        self.cmd = ""

    def encode(self):
        buf = BytesIO()
        buf.write(Debug._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __cmd_encoded = self.cmd.encode('utf-8')
        buf.write(struct.pack('>I', len(__cmd_encoded)+1))
        buf.write(__cmd_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Debug._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Debug._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = Debug()
        __cmd_len = struct.unpack('>I', buf.read(4))[0]
        self.cmd = buf.read(__cmd_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if Debug in parents: return 0
        tmphash = (0x2adc2162703541c8) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if Debug._packed_fingerprint is None:
            Debug._packed_fingerprint = struct.pack(">Q", Debug._get_hash_recursive([]))
        return Debug._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Debug._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.cmd = j["cmd"]

    def to_dict(self):
        j = {}
        j["cmd"] = self.cmd
        return j

