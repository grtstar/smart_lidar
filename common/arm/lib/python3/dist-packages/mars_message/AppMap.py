"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct
import json

import mars_message.Polygon

import mars_message.AppRoomProperties

import mars_message.GridMap

import mars_message.Pose

class AppMap(object):
    __slots__ = ["version", "mapId", "type", "map", "charger", "roomNum", "roomPropeties", "roomName", "roomPolygon"]

    __typenames__ = ["int8_t", "int16_t", "int8_t", "mars_message.GridMap", "mars_message.Pose", "int16_t", "mars_message.AppRoomProperties", "string", "mars_message.Polygon"]

    __dimensions__ = [None, None, None, None, None, None, ["roomNum"], ["roomNum"], ["roomNum"]]

    def __init__(self):
        self.version = 0
        self.mapId = 0
        self.type = 0
        self.map = mars_message.GridMap()
        self.charger = mars_message.Pose()
        self.roomNum = 0
        self.roomPropeties = []
        self.roomName = []
        self.roomPolygon = []

    def encode(self):
        buf = BytesIO()
        buf.write(AppMap._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bhb", self.version, self.mapId, self.type))
        assert self.map._get_packed_fingerprint() == mars_message.GridMap._get_packed_fingerprint()
        self.map._encode_one(buf)
        assert self.charger._get_packed_fingerprint() == mars_message.Pose._get_packed_fingerprint()
        self.charger._encode_one(buf)
        buf.write(struct.pack(">h", self.roomNum))
        for i0 in range(self.roomNum):
            assert self.roomPropeties[i0]._get_packed_fingerprint() == mars_message.AppRoomProperties._get_packed_fingerprint()
            self.roomPropeties[i0]._encode_one(buf)
        for i0 in range(self.roomNum):
            __roomName_encoded = self.roomName[i0].encode('utf-8')
            buf.write(struct.pack('>I', len(__roomName_encoded)+1))
            buf.write(__roomName_encoded)
            buf.write(b"\0")
        for i0 in range(self.roomNum):
            assert self.roomPolygon[i0]._get_packed_fingerprint() == mars_message.Polygon._get_packed_fingerprint()
            self.roomPolygon[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != AppMap._get_packed_fingerprint():
            raise ValueError("Decode error")
        return AppMap._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = AppMap()
        self.version, self.mapId, self.type = struct.unpack(">bhb", buf.read(4))
        self.map = mars_message.GridMap._decode_one(buf)
        self.charger = mars_message.Pose._decode_one(buf)
        self.roomNum = struct.unpack(">h", buf.read(2))[0]
        self.roomPropeties = []
        for i0 in range(self.roomNum):
            self.roomPropeties.append(mars_message.AppRoomProperties._decode_one(buf))
        self.roomName = []
        for i0 in range(self.roomNum):
            __roomName_len = struct.unpack('>I', buf.read(4))[0]
            self.roomName.append(buf.read(__roomName_len)[:-1].decode('utf-8', 'replace'))
        self.roomPolygon = []
        for i0 in range(self.roomNum):
            self.roomPolygon.append(mars_message.Polygon._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if AppMap in parents: return 0
        newparents = parents + [AppMap]
        tmphash = (0x3b81f63c979f13df+ mars_message.GridMap._get_hash_recursive(newparents)+ mars_message.Pose._get_hash_recursive(newparents)+ mars_message.AppRoomProperties._get_hash_recursive(newparents)+ mars_message.Polygon._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if AppMap._packed_fingerprint is None:
            AppMap._packed_fingerprint = struct.pack(">Q", AppMap._get_hash_recursive([]))
        return AppMap._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", AppMap._get_packed_fingerprint())[0]

    def from_dict(self, j):
        self.version = j["version"]
        self.mapId = j["mapId"]
        self.type = j["type"]
        self.map.from_dict(j["map"])
        self.charger.from_dict(j["charger"])
        self.roomNum = j["roomNum"]
        self.roomPropeties = [x.from_dict() for x in j["roomPropeties"]]
        self.roomName = j["roomName"]
        self.roomPolygon = [x.from_dict() for x in j["roomPolygon"]]

    def to_dict(self):
        j = {}
        j["version"] = self.version
        j["mapId"] = self.mapId
        j["type"] = self.type
        j["map"] = self.map.to_dict()
        j["charger"] = self.charger.to_dict()
        j["roomNum"] = self.roomNum
        j["roomPropeties"] = [x.to_dict() for x in self.roomPropeties]
        j["roomName"] = self.roomName
        j["roomPolygon"] = [x.to_dict() for x in self.roomPolygon]
        return j

