"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class CleanSpeed(object):
    __slots__ = ["zigzagV", "aroundV"]

    __typenames__ = ["float", "float"]

    __dimensions__ = [None, None]

    def __init__(self):
        self.zigzagV = 0.0
        self.aroundV = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(CleanSpeed._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">ff", self.zigzagV, self.aroundV))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != CleanSpeed._get_packed_fingerprint():
            raise ValueError("Decode error")
        return CleanSpeed._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = CleanSpeed()
        self.zigzagV, self.aroundV = struct.unpack(">ff", buf.read(8))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if CleanSpeed in parents: return 0
        tmphash = (0x5de50e64a7bed49) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if CleanSpeed._packed_fingerprint is None:
            CleanSpeed._packed_fingerprint = struct.pack(">Q", CleanSpeed._get_hash_recursive([]))
        return CleanSpeed._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", CleanSpeed._get_packed_fingerprint())[0]

